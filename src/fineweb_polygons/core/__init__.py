"""Direction-agnostic building blocks shared by every research direction.

Nothing in this package knows about a direction or a version. Directions
depend on `core`; `core` never depends on a direction.
"""
